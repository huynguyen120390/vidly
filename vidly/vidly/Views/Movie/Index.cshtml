@model IEnumerable<vidly.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customer</h2>
<table id="customers" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th scope="col">Movie</th>
            <th>Genre</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var movie in Model)
            {   <tr>
                    <td>
                        @Html.ActionLink(linkText:@movie.Name, actionName:"Details", controllerName:"Movie", routeValues: new {id=@movie.Id}, htmlAttributes:null)
                    </td>
                    <td>@movie.Genre.Name</td>
                </tr>
            }
        }
    </tbody>
</table>

